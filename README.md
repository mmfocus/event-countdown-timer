# MMF Timer — Complete iPad App Files

Upload ALL files in this folder to the root of your GitHub Pages repository.

The app is `index.html`.

The icon files are included so the MMF TIMER logo appears as the iPad Home Screen icon when the website is added to the Home Screen.

No external libraries or dependencies are required.
